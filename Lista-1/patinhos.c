#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    int P, M, N;
    char caminho[100000000];
    scanf("%s", &P);
    while(scanf("%d", caminho) != EOF) {
        
        }
    return 0;
}